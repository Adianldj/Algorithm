package BloombergOnsite;

public class findX {
//	x,y皆为int，我有一个function: y = f(x)，x,y正相关且one-to-one。通过 getY(int x) 可以知道x对应y的值。现在给你个y的值，请告诉我x值是多少（x 值保证存在）。
//			我的做法：
//			1. 在计算机int取值范围内，binary search一遍就好了，O (log N) 时间解决， N是计算机int取值范围。(binary search , 通过getY确定x的取值范围)
//			2. 对方法一的常数优化：先pre-compute一些y的值，存下来，减少二分查找范围。

}
